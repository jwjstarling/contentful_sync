name: contentful_sync
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.3

# Add regular dependencies here.
dependencies:
  http: ^1.1.0
  shared_preferences: ^2.0.6
  sqflite: ^2.0.0+3
  path_provider: ^2.0.2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
